#ifndef LOCAL_OBS_CONFIG_H
#define LOCAL_OBS_CONFIG_H

#include <cstddef>
#include <cstdint>

// Auto-generated by convert_local_only_to_esp32.py
// Architecture: LOCAL-ONLY (no latent input)

enum class LocalObsField : uint8_t {
    ProjectedGravity,
    Gyro,
    DofPos,
    DofVel,
};

// LOCAL-ONLY: No latent input — the MLP takes only local observations
constexpr size_t LOCAL_LATENT_DIM = 0;
constexpr size_t LOCAL_FRAME_HISTORY_STEPS = 10;
constexpr size_t LOCAL_LATENT_HISTORY_STEPS = 0;
constexpr size_t LOCAL_FRAME_COMPONENT_COUNT = 4;
constexpr LocalObsField LOCAL_FRAME_COMPONENTS[LOCAL_FRAME_COMPONENT_COUNT] = {
    LocalObsField::ProjectedGravity,
    LocalObsField::Gyro,
    LocalObsField::DofPos,
    LocalObsField::DofVel
};
constexpr size_t LOCAL_FRAME_DIM = 8;
constexpr size_t LOCAL_OBS_DIM = 80;

#endif // LOCAL_OBS_CONFIG_H
