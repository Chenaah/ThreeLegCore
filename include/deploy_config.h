#ifndef DEPLOY_CONFIG_H
#define DEPLOY_CONFIG_H

// Auto-generated by convert_local_only_to_esp32.py
// Architecture: LOCAL-ONLY (no master policy, no latent)
// Training config: local_only/config.yaml

constexpr int DEPLOY_NUM_MODULES = 3;

// Per-module default joint position offset (radians)
// Module 0: 0.0000 rad
// Module 1: 0.5000 rad
// Module 2: -0.5000 rad
constexpr float DEPLOY_DEFAULT_DOF_POS[3] = {0.000000f, 0.500000f, -0.500000f};

// PD gains used in training
constexpr float DEPLOY_KP = 8.0f;
constexpr float DEPLOY_KD = 0.2f;

// Action space bounds
constexpr float DEPLOY_ACTION_SCALE = 1.0f;
constexpr float DEPLOY_SYMMETRIC_LIMIT = 0.8f;

// Sanity check reference values
constexpr float DEPLOY_SANITY_EXPECTED_MEAN = -63.00809860f;
constexpr float DEPLOY_SANITY_EXPECTED_ACTION = -1.00000000f;

// Policy hash (FNV1a over all weight+bias bytes, masked to 31 bits)
constexpr int32_t DEPLOY_POLICY_HASH = 1431579494;

#endif // DEPLOY_CONFIG_H
